-- 创建数据库
CREATE DATABASE IF NOT EXISTS memo_app CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE memo_app;

-- 创建用户（可选，如果需要特定用户）
-- CREATE USER 'memo_user'@'localhost' IDENTIFIED BY '123456';
-- GRANT ALL PRIVILEGES ON memo_app.* TO 'memo_user'@'localhost';
-- FLUSH PRIVILEGES;
